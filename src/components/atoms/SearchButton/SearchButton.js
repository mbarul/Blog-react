import React from 'react';
import { StyledSearchButton } from './SearchButton.styles';

/**
 * @author
 * @function SearchButton
 **/

const SearchButton = (props) => {
  return <StyledSearchButton></StyledSearchButton>;
};

export default SearchButton;
